# Test Daily Digest

# Ejecutar locally:
curl -H "Authorization: Bearer YOUR_CRON_SECRET" \
  http://localhost:3000/api/cron/daily-digest

# Ejecutar en production:
curl -H "Authorization: Bearer YOUR_CRON_SECRET" \
  https://viaje-con-inteligencia.vercel.app/api/cron/daily-digest

# CRON_SECRET está configurado en Vercel Dashboard (no en repo por seguridad)